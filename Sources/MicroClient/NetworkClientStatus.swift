/// The network client status.
///
/// There are two states and they represent if the network
/// client is perform a network request or not.
public enum NetworkClientStatus {
    case running
    case idle
}
